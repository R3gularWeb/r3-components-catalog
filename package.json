{
  "scripts": {
    "storybook": "tsc && concurrently -k -r \"tsc --watch --preserveWatchOutput\" \"wds -c .storybook/server.mjs\"",
    "storybook:build": "tsc && build-storybook",
    "start": "tsc && concurrently -k -r \"tsc --watch --preserveWatchOutput\" \"wds\""
  },
  "devDependencies": {
    "@web/dev-server-storybook": "^0.3.5",
    "@web/dev-server": "^0.1.17",
    "concurrently": "^5.3.0",
    "typescript": "^4.3.4",
    "tslib": "^2.3.0"
  },
  "name": "r3-components-catalog",
  "version": "0.0.0",
  "description": "Webcomponent r3-components-catalog following open-wc recommendations",
  "author": "r3-components-catalog",
  "license": "MIT",
  "dependencies": {
    "lit-html": "^1.4.1",
    "lit-element": "^2.5.1"
  }
}